% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leading.R
\name{leadingVegTypeByAgeClass}
\alias{leadingVegTypeByAgeClass}
\title{Tabulate leading vegetation cover by age class for one vtm/sam pair}
\usage{
leadingVegTypeByAgeClass(
  vtm,
  sam,
  summaryPolys,
  polyCol,
  id,
  ageClasses = .ageClasses,
  ageClassCutOffs = .ageClassCutOffs
)
}
\arguments{
\item{vtm}{character, specifying the file path to a vegetation type map raster (\code{.tif})}

\item{sam}{character, specifying the file path to a stand age map raster (\code{.tif})}

\item{summaryPolys}{\code{sf} object containing the reporting polygons over which
results are summarised. Each polygon defines one reporting zone.}

\item{polyCol}{character, specifying the polygon attribute (column) in \code{summaryPolys} to use}

\item{id}{character or numeric identifier for this replicate, stored in
the \code{group} column of the output.}

\item{ageClasses}{character vector of age class labels, youngest to oldest.
Defaults to \code{c("Young", "Immature", "Mature", "Old")}.}

\item{ageClassCutOffs}{integer vector of lower age bounds (years) for each
class in \code{ageClasses}. Defaults to \code{c(0L, 40L, 80L, 120L)}.}
}
\value{
\code{data.table} with columns \code{group}, \code{zone}, \code{vegCover}, \code{ageClass},
\code{NPixels}, and \code{proportion}. \code{proportion} is the fraction of that
species' pixels (within the polygon) in each age class; for
\code{vegCover == "All_species"} it is the fraction of all forested pixels
in each age class.
}
\description{
For each reporting polygon in \code{summaryPolys}, crops and masks the supplied
vegetation type and stand age rasters, counts pixels per species × age-class
combination, and computes the proportion of each species' pixels that fall
in each age class. A separate "All_species" row gives the proportion of all
forested pixels in each age class irrespective of species. Missing
combinations are filled with zeros so every zone × species × age-class
triple is always present in the output.
}
\seealso{
\code{\link[=calculateLeadingVegTypeByAgeClass]{calculateLeadingVegTypeByAgeClass()}}
}
