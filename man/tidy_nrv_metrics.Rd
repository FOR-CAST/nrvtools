% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{tidy_nrv_metrics}
\alias{tidy_nrv_metrics}
\title{Bind a raw per-replicate metric list into one tidy long table}
\usage{
tidy_nrv_metrics(metric_list, studyArea = NULL, scenario = NULL)
}
\arguments{
\item{metric_list}{A named \code{list} of raw metric \code{data.frame}s, or a single
\code{data.frame}. \code{NULL}/empty elements are dropped; an all-empty input returns a
zero-row \code{data.frame}.}

\item{studyArea, scenario}{Optional length-1 identifiers prepended as columns
(when not \code{NULL}).}
}
\value{
A long \code{data.frame} (the row-bind of \code{metric_list}), with \code{studyArea}
and/or \code{scenario} prepended when supplied.
}
\description{
The raw metric producers (\code{\link[=nrv_metrics_landscape]{nrv_metrics_landscape()}}, \code{\link[=calculatePatchMetrics]{calculatePatchMetrics()}},
\code{\link[=calculatePatchMetricsSeral]{calculatePatchMetricsSeral()}}) each return a named \code{list} -- one element per
entry in \code{funList} -- of long \code{data.frame}s that already share the columns
\code{level}, \code{class}, \code{metric}, \code{value}, \code{rep}, \code{time}, \code{poly} (the
\pkg{landscapemetrics} \code{metric} name together with \code{level} uniquely identifies
each metric, so the \code{funList} element name is not carried as a separate column).
This binds them into a single long table ready for \code{\link[=write_nrv_parquet]{write_nrv_parquet()}},
optionally stamping run-level identifiers so a union of several runs can be
grouped by them in \code{\link[=summarize_nrv]{summarize_nrv()}}.
}
