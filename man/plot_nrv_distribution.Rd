% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_nrv_distribution}
\alias{plot_nrv_distribution}
\title{Plot the across-replicate distribution of a metric, with a reference line}
\usage{
plot_nrv_distribution(
  x,
  cc = NULL,
  value_col = "value",
  facet = c("rptPoly", "poly", "class", "metric", "metric.1"),
  bins = 30,
  xlab = value_col,
  ylab = "Proportion in NRV"
)
}
\arguments{
\item{x}{Raw per-replicate values: parquet file paths / a dataset root (see
\code{\link[=open_nrv_dataset]{open_nrv_dataset()}}), an Arrow \code{Dataset}/query, or a \code{data.frame}.}

\item{cc}{Optional current-condition values (same forms as \code{x}); their per-panel
value is drawn as a red vertical reference line. \code{NULL} (default) omits it.}

\item{value_col}{Name of the value column to bin (default \code{"value"}).}

\item{facet}{Candidate faceting columns; those present and varying become the
panel label (default \code{c("rptPoly", "poly", "class", "metric", "metric.1")}).}

\item{bins}{Number of histogram bins (default \code{30}).}

\item{xlab, ylab}{Axis labels.}
}
\value{
A \code{ggplot} object, or \code{NULL} for empty input.
}
\description{
The LandWeb-summary counterpart to \code{\link[=plot_nrv_envelope]{plot_nrv_envelope()}}: instead of an
envelope over \code{time}, it plots the \emph{distribution across replicates} (pooled over
the summary period) of the raw per-replicate values as a histogram, one panel
per faceting-column combination, with an optional red vertical reference line at
the current-condition value. This reproduces the v2 \code{LandWeb_summary} histograms
("Proportion in NRV" vs the metric, with the current-condition marker), which
are deliberately not time series.
}
\details{
Because the histogram needs the raw per-replicate values (not the collapsed
envelope), pass the replicate parquet(s) -- as produced by \code{\link[=write_nrv_parquet]{write_nrv_parquet()}}
-- rather than a \code{\link[=summarize_nrv]{summarize_nrv()}} envelope.
}
\seealso{
\code{\link[=plot_nrv_envelope]{plot_nrv_envelope()}}, \code{\link[=calculateLandWebMetrics]{calculateLandWebMetrics()}}
}
