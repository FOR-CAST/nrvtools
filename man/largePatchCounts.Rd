% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landweb-summaries.R
\name{largePatchCounts}
\alias{largePatchCounts}
\title{Count of large patches by size class, vegetation class and age class}
\usage{
largePatchCounts(
  vtm,
  age,
  ageClassCutOffs,
  ageClasses,
  sizeClasses = c(100, 500, 1000, 5000),
  minSize = 100,
  directions = 4L
)
}
\arguments{
\item{vtm}{character, specifying the file path to a vegetation type map raster (\code{.tif})}

\item{age}{character file path (or a \code{SpatRaster}) of an age raster used for
age-class binning: a time-since-fire map (v2 LandWeb_summary semantics) or a
stand-age map.}

\item{ageClassCutOffs}{numeric, the lower bounds of each age class (same length
as \code{ageClasses}), e.g. \code{c(0, 40, 80, 120)}.}

\item{ageClasses}{character, the age-class labels, e.g.
\code{c("Young", "Immature", "Mature", "Old")}.}

\item{sizeClasses}{numeric, patch-size thresholds in hectares to tabulate,
e.g. \code{c(100, 500, 1000, 5000)}.}

\item{minSize}{numeric, the minimum patch size (ha) retained before tabulation
(default \code{100}).}

\item{directions}{patch connectivity passed to
\code{\link[landscapemetrics:get_patches]{landscapemetrics::get_patches()}}: \code{4} (rook, matches v2) or \code{8} (queen).}
}
\value{
a raw long \code{data.frame} with columns \code{layer}, \code{level}, \code{class} (the age
class), \code{id}, \code{metric} (\code{"Npatch_ge<size>ha"} / \code{"totalArea_ge<size>ha"}),
\code{metric.1} (the vegetation class, incl. \code{"All species"}) and \code{value}.
}
\description{
Ports the v2 \code{LandWebUtils::LargePatches()} + \code{runHistsLargePatches()} analysis
onto the \pkg{nrvtools} raw-producer contract. For a single (already
reporting-polygon cropped) vegetation-type map and age raster, it bins the age
raster into age classes, forms one categorical raster keyed by the
(age class x vegetation class) combination, delineates contiguous same-key
patches with \code{\link[landscapemetrics:get_patches]{landscapemetrics::get_patches()}} (\code{directions} connectivity,
default 4 to match the v2 GDAL polygonize), and for each \code{sizeClasses} threshold
counts the number of patches at least that large and their total area, per
(age class x vegetation class). An \code{"All species"} pass keys patches on age
class only. Patches smaller than \code{minSize} ha are dropped, as in v2.
}
\seealso{
\code{\link[=leadingVegByAgeClass]{leadingVegByAgeClass()}}, \code{\link[=calculateLandWebMetrics]{calculateLandWebMetrics()}}
}
