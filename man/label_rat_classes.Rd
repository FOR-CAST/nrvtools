% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patches.R
\name{label_rat_classes}
\alias{label_rat_classes}
\alias{label_vegtype_classes}
\title{Relabel integer class codes with their raster attribute-table labels}
\usage{
label_rat_classes(df, ras, class_col = "class")

label_vegtype_classes(df, vtm, class_col = "class")
}
\arguments{
\item{df}{A \code{data.frame}/\code{tibble} with a class column (\code{class_col}); returned
unchanged if \code{NULL}, empty, or lacking that column.}

\item{ras}{A categorical \code{SpatRaster}; its RAT supplies the code -> label
lookup. A raster with no usable RAT leaves \code{df} unchanged.}

\item{class_col}{Name of the class column to relabel (default \code{"class"}).}

\item{vtm}{A categorical vegetation-type \code{SpatRaster} (leading-species map).}
}
\value{
\code{df} with \code{class_col} relabelled where codes matched the RAT.
}
\description{
Replaces integer category codes in a \code{class} column with the matching labels
from a categorical raster's RAT (\code{terra::levels(ras)[[1]]}). Class-level
\pkg{landscapemetrics} metrics (\verb{lsm_c_*()}) report the raw integer category,
whereas the nrvtools producers (\code{\link[=patchAges]{patchAges()}} / \code{\link[=patchAreas]{patchAreas()}} /
\code{\link[=patchAreasSeral]{patchAreasSeral()}}) already return labelled classes -- this relabels the
former and leaves the latter unchanged. Idempotent: any class value that is
not an integer code (e.g. an already-mapped label) or that has no RAT match is
kept as-is. Handles a \code{class} column stored as numeric (raw metric output) or
as character (e.g. after a mixed-type column is written to and read back from
parquet).
}
\details{
\code{label_vegtype_classes()} is the vegetation-type-map spelling of the same
operation (a leading-species map's RAT maps code -> species name);
\code{label_rat_classes()} applies to any categorical map, e.g. the seral-stage map
from \code{\link[=seralStageMapGeneratorBC]{seralStageMapGeneratorBC()}}, whose RAT maps code -> seral stage.
}
\seealso{
\code{\link[=patchStats]{patchStats()}}, \code{\link[=patchStatsSeral]{patchStatsSeral()}}, \code{\link[=patchAreas]{patchAreas()}}
}
