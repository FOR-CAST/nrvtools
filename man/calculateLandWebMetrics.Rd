% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landweb-summaries.R
\name{calculateLandWebMetrics}
\alias{calculateLandWebMetrics}
\title{Calculate LandWeb summary metrics (Leading + LargePatches), raw per replicate}
\usage{
calculateLandWebMetrics(summaryPolys, polyCol, vtm, age, funList = NULL, ...)
}
\arguments{
\item{summaryPolys}{\code{sf} polygons object}

\item{polyCol}{character, specifying the polygon attribute (column) in \code{summaryPolys} to use}

\item{vtm}{character, specifying the file path to a vegetation type map raster (\code{.tif})}

\item{age}{character file path (or a \code{SpatRaster}) of an age raster used for
age-class binning: a time-since-fire map (v2 LandWeb_summary semantics) or a
stand-age map.}

\item{funList}{optional list of character strings specifying functions to use for calculating
landscape or patch statistics (e.g., \code{list("lsm_l_area_mn")}).
These functions must work analogously to those in \pkg{landscapemetrics}.
If not specified by the user (i.e., \code{funList = NULL}), default function names are used:
see \code{\link[=default_landscape_metrics]{default_landscape_metrics()}}, \code{\link[=default_patch_metrics]{default_patch_metrics()}}, \code{\link[=default_patch_metrics_seral]{default_patch_metrics_seral()}}.}

\item{...}{further arguments passed to the producers (e.g. \code{ageClassCutOffs},
\code{ageClasses}, \code{sizeClasses}, \code{minSize}, \code{directions}, \code{sppEquiv},
\code{sppEquivCol}); each producer receives only the arguments matching its formals.}
}
\value{
a named \code{list} (one element per entry in \code{funList}), each a raw long
\code{data.frame} with columns \code{layer}, \code{level}, \code{class}, \code{id}, \code{metric},
\code{metric.1}, \code{value}, \code{rep}, \code{time}, \code{poly}.
}
\description{
The \pkg{nrvtools} raw-producer wrapper for the ported LandWeb_summary analyses,
analogous to \code{\link[=calculatePatchMetrics]{calculatePatchMetrics()}} but (a) passing both the vegetation-type
map and an age (time-since-fire or stand-age) raster to each producer, and (b)
\emph{not} masking non-flammable pixels (matching v2 \code{LargePatches}/\code{LeadingVegType}).
For each vegetation-type map in \code{vtm} (aligned element-wise with \code{age}), within
each summary polygon it crops+masks the maps to the polygon and runs each
producer in \code{funList}, returning the raw per-replicate long tables (one row per
replicate x time x polygon x class x metric) for \code{\link[=tidy_nrv_metrics]{tidy_nrv_metrics()}} ->
\code{\link[=write_nrv_parquet]{write_nrv_parquet()}} -> \code{\link[=summarize_nrv]{summarize_nrv()}}.
}
\details{
The NRV envelope for these metrics pools across replicates \emph{and} summary years,
so summarize with \code{time} excluded from the grouping id columns.
}
\seealso{
\code{\link[=leadingVegByAgeClass]{leadingVegByAgeClass()}}, \code{\link[=largePatchCounts]{largePatchCounts()}}, \code{\link[=calculatePatchMetrics]{calculatePatchMetrics()}}
}
