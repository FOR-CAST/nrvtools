% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_largepatch_histogram}
\alias{plot_largepatch_histogram}
\title{Large-patch age-class histogram (v2 LandWeb_summary form, one file per species)}
\usage{
plot_largepatch_histogram(
  df,
  cc = NULL,
  ageClasses = c("Young", "Immature", "Mature", "Old"),
  value_col = "value",
  class_col = "class",
  bins = 30,
  xlab = "Number of patches",
  title = NULL
)
}
\arguments{
\item{df}{A \code{data.frame} of the raw per-replicate values for one
species x size unit, with an age-class column (\code{class_col}) and a count
column (\code{value_col}); one row per replicate x summary-year x age class.}

\item{cc}{Optional \code{data.frame} of current-condition values (same columns),
one per age class, drawn as red vertical lines. \code{NULL} omits them.}

\item{ageClasses}{Age-class labels in order young -> old (panel order).}

\item{value_col, class_col}{Column names for the count and age class.}

\item{bins}{Number of histogram bins (default \code{30}).}

\item{xlab, title}{Axis label and plot title.}
}
\value{
A \code{ggplot} object (\code{NULL} for empty input).
}
\description{
Reproduces the v2 "LargePatches" histogram for a single reporting unit
(subregion x species x size threshold), but grouped one file per species with
\strong{four age-class panels} (young -> old) instead of one file per age class.
Each panel is the across-replicate distribution ("Proportion in NRV") of the
number of patches at or above the size threshold, with the current-condition
count marked as a red vertical line.
}
\seealso{
\code{\link[=plot_leading_boxplot]{plot_leading_boxplot()}}, \code{\link[=largePatchCounts]{largePatchCounts()}}
}
