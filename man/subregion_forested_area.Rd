% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patches.R
\name{subregion_forested_area}
\alias{subregion_forested_area}
\title{Forested area (ha) per subregion and leading species}
\usage{
subregion_forested_area(vtm, polys, poly_col, all_label = "All species")
}
\arguments{
\item{vtm}{A categorical vegetation-type \code{SpatRaster} (leading-species map);
assumed to have a projected CRS in metres (cell area = \code{prod(res(vtm))}).}

\item{polys}{Subregion polygons (\code{SpatVector}, \code{sf}, or \code{Spatial}); reprojected
to \code{vtm} as needed.}

\item{poly_col}{Name of the column in \code{polys} holding the subregion label.}

\item{all_label}{Label for the per-subregion total row (default
\code{"All species"}); \code{NULL} omits the totals.}
}
\value{
A \code{data.frame} with columns \code{poly}, \code{vegCover}, \code{n_pixels}, \code{area_ha}.
}
\description{
Tabulates the forested area, in hectares, of each leading-vegetation species
within each subregion of a reporting layer, from a categorical
vegetation-type map (the leading-species map). Mirrors the v2 LandWeb /
NW_AB comparative-boxplot area calculation: extract the map over the
subregion polygons, count cells per species, and multiply by the cell area.
An \code{all_label} ("All species") row per subregion carries the subregion's
total forested area.
}
\seealso{
\code{\link[=plot_leading_boxplot]{plot_leading_boxplot()}}
}
