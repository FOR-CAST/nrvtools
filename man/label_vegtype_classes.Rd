% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patches.R
\name{label_vegtype_classes}
\alias{label_vegtype_classes}
\title{Relabel integer vegetation-type class codes with their species labels}
\usage{
label_vegtype_classes(df, vtm, class_col = "class")
}
\arguments{
\item{df}{A \code{data.frame}/\code{tibble} with a class column (\code{class_col}); returned
unchanged if \code{NULL}, empty, or lacking that column.}

\item{vtm}{A categorical vegetation-type \code{SpatRaster}; its RAT supplies the
code -> label lookup. A raster with no usable RAT leaves \code{df} unchanged.}

\item{class_col}{Name of the class column to relabel (default \code{"class"}).}
}
\value{
\code{df} with \code{class_col} relabelled where codes matched the RAT.
}
\description{
Replaces integer vegetation-type category codes in a \code{class} column with the
matching species labels from a categorical vegetation-type map's RAT
(\code{terra::levels(vtm)[[1]]}). Class-level \pkg{landscapemetrics} metrics
(\verb{lsm_c_*()}) report the raw integer category, whereas the nrvtools producers
(\code{\link[=patchAges]{patchAges()}} / \code{\link[=patchAreas]{patchAreas()}}) already return labelled classes -- this
relabels the former and leaves the latter unchanged. Idempotent: any class
value that is not an integer code (e.g. an already-mapped species name) or
that has no RAT match is kept as-is. Handles a \code{class} column stored as
numeric (raw metric output) or as character (e.g. after a mixed-type column is
written to and read back from parquet).
}
\seealso{
\code{\link[=patchStats]{patchStats()}}, \code{\link[=patchAreas]{patchAreas()}}
}
