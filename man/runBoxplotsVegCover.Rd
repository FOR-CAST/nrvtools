% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vegcover.R
\name{runBoxplotsVegCover}
\alias{runBoxplotsVegCover}
\title{Generate box and whisker plots for leading vegetation cover}
\usage{
runBoxplotsVegCover(
  summaryPolys,
  polyCol,
  vtm,
  sam,
  dPath,
  ccPattern = "CC",
  pixelRes = NULL,
  ageClasses = .ageClasses,
  ageClassCutOffs = .ageClassCutOffs
)
}
\arguments{
\item{summaryPolys}{\code{sf} object containing the reporting polygons over which
results are summarised. Each polygon defines one reporting zone.}

\item{polyCol}{character, specifying the polygon attribute (column) in \code{summaryPolys} to use}

\item{vtm}{character, specifying the file path to a vegetation type map raster (\code{.tif})}

\item{sam}{character, specifying the file path to a stand age map raster (\code{.tif})}

\item{dPath}{character, destination path for the resulting CSV and PNG files.}

\item{ccPattern}{character, regex pattern identifying current-condition
replicates within the group names derived from \code{basename(dirname(vtm))}.
Defaults to \code{"CC"}.}

\item{pixelRes}{optional numeric vector of length 2 giving pixel resolution
in metres (e.g., \code{c(250, 250)}). When supplied, the x-axis label includes
the total forested area in hectares.}

\item{ageClasses}{character vector of age class labels, youngest to oldest.
Defaults to \code{c("Young", "Immature", "Mature", "Old")}.}

\item{ageClassCutOffs}{integer vector of lower age bounds (years) for each
class in \code{ageClasses}. Defaults to \code{c(0L, 40L, 80L, 120L)}.}
}
\value{
named list (one element per reporting polygon) of \code{data.table} objects
with columns \code{zone}, \code{vegCover}, and \code{filename} giving PNG output paths.
Invoked primarily for the side effects of writing CSV and PNG files.
}
\description{
For each reporting polygon in \code{summaryPolys}, crops and masks the supplied
vegetation type and stand age rasters to that polygon, tabulates leading
species by age class across simulation replicates, and writes box-and-whisker
plots (PNG) and summary tables (CSV) to \code{dPath}.
}
\details{
Groups whose name matches \code{ccPattern} are treated as current-condition (CC)
reference runs and overlaid as red points on each plot.
}
