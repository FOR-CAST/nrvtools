% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landscape.R
\name{nrv_metrics_landscape}
\alias{nrv_metrics_landscape}
\title{Calculate landscape metrics (raw, per replicate)}
\usage{
nrv_metrics_landscape(summaryPolys, polyCol, vtm, funList = NULL)
}
\arguments{
\item{summaryPolys}{\code{sf} polygons object}

\item{polyCol}{character, specifying the polygon attribute (column) in \code{summaryPolys} to use}

\item{vtm}{character, specifying the file path to a vegetation type map raster (\code{.tif})}

\item{funList}{optional list of character strings specifying functions to use for calculating
landscape or patch statistics (e.g., \code{list("lsm_l_area_mn")}).
These functions must work analogously to those in \pkg{landscapemetrics}.
If not specified by the user (i.e., \code{funList = NULL}), default function names are used:
see \code{\link[=default_landscape_metrics]{default_landscape_metrics()}}, \code{\link[=default_patch_metrics]{default_patch_metrics()}}, \code{\link[=default_patch_metrics_seral]{default_patch_metrics_seral()}}.}
}
\value{
a named \code{list} (one element per entry in \code{funList}), each a raw long
\code{data.frame} with columns \code{layer}, \code{level}, \code{class}, \code{id}, \code{metric}, \code{value},
\code{rep}, \code{time}, \code{poly}.
}
\description{
Computes the requested landscape-level \pkg{landscapemetrics} for each
vegetation-type map in \code{vtm} within each summary polygon, returning the \emph{raw}
per-replicate values (one row per replicate x time x polygon x metric). No
across-replicate reduction is performed here: pass the result (via
\code{\link[=tidy_nrv_metrics]{tidy_nrv_metrics()}} / \code{\link[=write_nrv_parquet]{write_nrv_parquet()}}) to \code{\link[=summarize_nrv]{summarize_nrv()}} to build the
range-of-variation envelope. Keeping the producer raw is what makes the summary
path memory-bounded -- each replicate's raw table can be written to its own
parquet partition and the reduction pushed down to Arrow compute, instead of
binding every replicate into memory before summarising.
}
\details{
\code{vtm} file paths encode the replicate/time/polygon the way the summary path
expects: the parent directory is the replicate (\verb{rep<NN>}) and the file name is
\verb{<prefix>_year<YYYY>.tif}.
}
