% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/largePatches.R
\name{largePatches}
\alias{largePatches}
\title{Calculate large patch areas for one vtm/sam pair}
\usage{
largePatches(
  vtm,
  sam,
  summaryPolys,
  polyCol,
  id,
  ageClasses = .ageClasses,
  ageClassCutOffs = .ageClassCutOffs,
  minPatchSize = 100
)
}
\arguments{
\item{vtm}{character, specifying the file path to a vegetation type map raster (\code{.tif})}

\item{sam}{character, specifying the file path to a stand age map raster (\code{.tif})}

\item{summaryPolys}{\code{sf} object containing the reporting polygons over which
results are summarised. Each polygon defines one reporting zone.}

\item{polyCol}{character, specifying the polygon attribute (column) in \code{summaryPolys} to use}

\item{id}{character or numeric identifier for this replicate, stored in the
\code{rep} column of the output.}

\item{ageClasses}{character vector of age class labels, youngest to oldest.
Defaults to \code{c("Young", "Immature", "Mature", "Old")}.}

\item{ageClassCutOffs}{integer vector of lower age bounds (years) for each
class in \code{ageClasses}. Defaults to \code{c(0L, 40L, 80L, 120L)}.}

\item{minPatchSize}{numeric, minimum patch size in hectares to retain.
Defaults to \code{100}.}
}
\value{
\code{data.table} with columns \code{vegCover}, \code{ageClass}, \code{polygonName},
\code{rep}, and \code{sizeInHa}. Rows with \code{vegCover == "All_species"} give patch
sizes summed across all species within each age class.
}
\description{
For each polygon in \code{summaryPolys}, classifies pixels by leading species
(from \code{vtm}) and age class (derived from \code{sam}), then identifies connected
patches of each species × age-class combination and computes their area in
hectares. A second pass computes patches across all species combined (per
age class only). Patches smaller than \code{minPatchSize} are discarded.
}
\seealso{
\code{\link[=calculateLargePatches]{calculateLargePatches()}}
}
