% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landweb-summaries.R
\name{leadingVegByAgeClass}
\alias{leadingVegByAgeClass}
\title{Proportion of a reporting area in each vegetation class by age class}
\usage{
leadingVegByAgeClass(vtm, age, ageClassCutOffs, ageClasses)
}
\arguments{
\item{vtm}{character, specifying the file path to a vegetation type map raster (\code{.tif})}

\item{age}{character file path (or a \code{SpatRaster}) of an age raster used for
age-class binning: a time-since-fire map (v2 LandWeb_summary semantics) or a
stand-age map.}

\item{ageClassCutOffs}{numeric, the lower bounds of each age class (same length
as \code{ageClasses}), e.g. \code{c(0, 40, 80, 120)}.}

\item{ageClasses}{character, the age-class labels, e.g.
\code{c("Young", "Immature", "Mature", "Old")}.}
}
\value{
a raw long \code{data.frame} with columns \code{layer}, \code{level}, \code{class} (the age
class), \code{id}, \code{metric} (\code{"leadingProp"}), \code{metric.1} (the vegetation class,
incl. \code{"All species"}) and \code{value} (the proportion).
}
\description{
Ports the v2 \code{LandWebUtils::LeadingVegTypeByAgeClass()} analysis onto the
\pkg{nrvtools} raw-producer contract. For a single (already reporting-polygon
cropped) vegetation-type map and age raster, it bins the age raster into age
classes, then for each leading vegetation class computes the proportion of that
class's pixels falling in each age class (plus an \code{"All species"} roll-up over
all classes), zero-filling every age-class x vegetation-class combination.
}
\details{
The vegetation-type map already encodes the leading species per pixel (produced
upstream by \code{LandR::vegTypeMapGenerator()} with \code{vegLeadingProportion}), so this
function does not recompute "leading".
}
\seealso{
\code{\link[=largePatchCounts]{largePatchCounts()}}, \code{\link[=calculateLandWebMetrics]{calculateLandWebMetrics()}}
}
